# Copy this file to .env and fill in real values.
# .env itself must be in .gitignore — never commit real credentials.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=wb_who_data
DB_USER=postgres
DB_PASSWORD=postgres
